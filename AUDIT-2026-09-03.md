# 根目录全面自检审计报告

**审计日期**：2026-09-03
**审计范围**：`G:\DSH\DSH-PluginUpdater` 根目录及全部子项目
**审计方式**：只读扫描（未删除、未修改任何业务文件）
**当前版本**：src-vue / src-tauri / website 三处一致，均为 1.13.8 ✓

---

## 一、执行摘要

共发现 **18 项问题**，其中 **3 项属立即处理级别**。

最严重的一条：**更新签名私钥已经躺在公开 GitHub 上**。任何人下载到这把私钥，就能伪造"官方插件市场目录"并让你的所有用户客户端无条件信任。这不是理论风险——代码里的签名验证目前还是关闭状态，等于大门敞开且锁芯已被复制。

第二严重：根目录那份 `AUDIT-REPORT.md`（8月29日）写着"密钥管理: OK（私钥已 gitignore）"，**这个结论是错的**。它只看了 .gitignore 有没有这条规则，没检查文件是否早已提交进历史。正是这份报告给了虚假的安全确认，导致问题被掩盖至今。

---

## 二、P0 级问题（立即处理）

### P0-1 更新签名私钥已泄露至公开仓库

| 项目 | 内容 |
|---|---|
| 泄露文件 | `scripts/ed25519-private.pem`、`scripts/key-info.json`（含明文私钥 PEM）、`src-tauri/keys/ed25519-private.bin` |
| 泄露提交 | `8932de1` feat: Ed25519 manifest 签名验证（信任根） |
| 传播范围 | **已推送到公开 GitHub** `Frank-NF/DSH-PluginUpdater`，同时也在私有 hub |
| 确认方式 | `git merge-base --is-ancestor 8932de1 origin/main` 返回真；本地与远端完全同步（0/0） |

**业务影响**：私钥是插件目录签名的"公章"。公章外流后，攻击者可以签发一份伪造的插件市场目录，把正常插件的下载地址替换成木马，你的客户端会认为"签名有效"而照单全收。这直接威胁到每一位使用你工具的用户。

**关键点**：`.gitignore` 里确实有 `scripts/ed25519-*.pem` 规则，但规则是在文件提交**之后**才加的——git 的忽略规则对已进入历史的文件无效。

**处理动作**（三步，缺一不可）：
1. **轮换密钥**：生成全新的 Ed25519 密钥对，替换 `scripts/ed25519-*.pem`、`src-tauri/keys/*.bin`，并同步更新 `src-tauri/src/catalog.rs` 内置的公钥。
2. **清除历史**：用 `git filter-repo`（或 BFG）把这三个文件从全部 67 个提交的历史中彻底抹掉。仅删除文件无效，历史里依然可提取。
3. **强制推送**：清理后需 `git push --force` 覆盖 GitHub 与 hub 两端。

> ⚠️ 因为已经公开传播，即使清理了历史，也应视为旧密钥已永久失效——**必须以轮换密钥为主，清理历史为辅**。GitHub 可能已被镜像抓取。

### P0-2 旧审计报告存在虚假安全结论

`AUDIT-REPORT.md`（8月29日）的"安全审计"章节写道：

> 密钥管理: OK (私钥已 gitignore)

事实是私钥早已提交并推送。该文件还声称"Ed25519 签名: 框架已实现，需服务器配合"，而实际上**验证代码整段被注释**（见 P1-3）。

**处理动作**：该文件结论不可采信，建议作废或重写，避免后续再次误导决策。

### P0-3 更新版本清单文件 `version.json` 在工作区丢失

- `git status` 显示 ` D version.json`——文件在最新提交 `a5ef183` 中存在，但磁盘上已被删除，且删除尚未提交。
- 该文件是自我更新功能的版本清单（`version` 1.13.8 + 下载 URL + sha256 + 变更说明）。

**业务影响**：文件缺失会导致发版流程无清单可依，自我更新接口返回空数据，用户端检测不到新版本。

**处理动作**：确认是有意删除还是误删。若为误删，执行 `git checkout HEAD -- version.json` 恢复；若为有意调整，需从版本库中正式移除并提交删除。

---

## 三、P1 级问题（尽快处理）

### P1-1 中转代理服务器源码完全不受版本控制

`.gitignore` 第 64 行将 `proxy-server/` 整个目录忽略。经核对，该目录下 **6 个文件无一在版本库中**：

- `main.go`（10KB 源码）
- `Dockerfile`、`docker-compose.yml`（部署配置）
- `go.mod`、`go.sum`（依赖锁定）
- `dsh-proxy.exe`（14MB 编译产物，此项忽略合理）

**业务影响**：Go 中转代理的源码和部署配置只存在于本机磁盘。一旦硬盘故障或误删，整套代理服务无法重建，依赖它的插件下载链路会中断。

**处理动作**：修改 `.gitignore`，改为只忽略编译产物（`proxy-server/*.exe`），把源码与部署配置纳入版本控制。

### P1-2 官网正式页面引用调试预览服务器

`website` 下有 6 处硬编码了调试预览地址 `http://64.90.30.139:8071/`：

- `components/SiteHeader.vue`、`SiteFooter.vue`
- `pages/index.vue`（2 处）、`download.vue`、`plugins.vue`

**业务影响**：官网的"在线版"入口指向的是一个临时调试服务器，且用明文 http 传输。该服务器一旦下线或目录清理，官网所有在线版入口**全线失效**；明文传输还存在被劫持风险。

**处理动作**：提取为环境变量（如 `NUXT_PUBLIC_PREVIEW_URL`），正式环境指向正式域名 + https。

### P1-3 插件目录签名验证代码被整段注释，从未生效

`src-tauri/src/main.rs` 第 465-485 行，签名验证逻辑整段处于 `/* ... */` 注释块中，注释里写着"当前版本暂不强校验，仅记录签名状态"。

**业务影响**：即便服务器返回了 `X-DSH-SIGNATURE` 签名头，客户端也**完全不验证**。插件目录的真实性和完整性没有任何保障。这条与 P0-1 叠加后，安全体系实际等同于零。

**处理动作**：签名验证正式启用前，必须先完成 P0-1 的密钥轮换，否则启用验证反而会信任攻击者用泄露私钥签的内容。

### P1-4 自我更新路径没有任何签名验证

`check_self_update()` 从 `https://dsh.huilinsh.cn/api/updater/latest` 拉取版本信息，`self_update()` 下载 exe 后仅校验 `expected_sha256`——**而这个 sha256 同样来自那个未经签名验证的接口**。

**业务影响**：若服务器被攻陷或域名被劫持，攻击者可同时返回恶意 exe 地址与匹配的 sha256，校验形同虚设，用户端会自动下载并替换自身为恶意程序。

**附带风险**：替换自身用的是 `cmd /c move /Y` 覆盖当前 exe，若程序安装在 `Program Files`，普通权限无法写入，更新会静默失败。

**处理动作**：
1. 对版本清单接口启用 Ed25519 签名（而非 sha256 自证）。
2. 更新替换改用可靠方案（官方 updater 插件，或带提权的独立更新程序）。

### P1-5 安装包目录版本严重脱节且存在冗余

`安装包/` 下四个文件全部停留在 **v1.0.0**（8月30日打包），而代码版本已是 **1.13.8**，中间隔了约 38 个版本迭代：

```
DSH插件升级管理-v1.0.0-old.msi          5.8 MB
DSH插件升级管理-v1.0.0-安装版-old.exe   4.1 MB
DSH插件升级管理-v1.0.0-安装版.exe       4.1 MB
DSH插件升级管理-v1.0.0.msi              5.8 MB
```

**业务影响**：文件名会让用户误以为产品还在 1.0 阶段，且带 `-old` 的过期副本混在其中，**极易发错包给用户**。

**处理动作**：按 1.13.8 重新打包并规范命名，清理 `-old` 副本（或移入归档目录）。

---

## 四、P2 级问题（计划处理）

### P2-1 编译缓存占用 16 GB

| 目录 | 体积 | 说明 |
|---|---|---|
| `src-tauri/target/debug` | 13 GB | 调试构建产物 |
| `src-tauri/target/release` | 3.2 GB | 发布构建产物 |
| **src-tauri 实际源码** | **276 KB** | 真实代码量 |

整个项目 16 GB，其中 **99.9% 是构建缓存**。此外 `website/node_modules` 255 MB、`src-vue/node_modules` 96 MB。

**处理动作**：`cargo clean` 可回收约 16 GB。代价是下次完整重新编译（通常 10-30 分钟）。建议在发版后执行，或仅清理 `target/debug`。

### P2-2 视觉识别临时截图混入版本库

`.dsh-vision-router/artifacts/` 下 **47 个临时截图**（1.8 MB）已被提交进版本库。忽略规则 `**/.dsh-vision-router/` 同样加晚了。

### P2-3 根目录散落文件未归档

| 文件 | 体积 | 建议归属 |
|---|---|---|
| `catalog-sample.json` | 1.8 MB | 插件目录样本（2189 款），移入 `docs/` 或 `samples/` |
| `ui-desktop-final.png`、`ui-toolbar-v2.png` | 366 KB | UI 截图，移入 `artifacts/` |
| `nginx-dsh-updater.conf` | 226 B | 服务器配置，移入 `docs/` 或 `deploy/` |
| `website-src.tar.gz` | 123 KB | 源码压缩包（已被忽略），确认无用可删 |
| `batch-check-body.json` | 2.3 KB | 调试用请求体样本 |
| `debug.log` | 130 B | WebView2 crashpad 报错残留 |

### P2-4 各类构建残留

- `src-tauri/icons-bak-1787935818314/`（77 KB，已被追踪的临时备份）
- `src-vue/dist.tar.gz`（444 KB）、`dist-preview/`、`dist-v3/`
- `src-vue/vite.config.ts.timestamp-*.mjs`（Vite 临时文件）

### P2-5 部署脚本硬编码服务器信息

`scripts/push-via-relay.ps1` 中硬编码了服务器 IP `64.90.30.139` 与 `root` 用户名，该文件已推送到公开仓库。属于信息暴露，建议改用环境变量或 SSH config 别名。

### P2-6 nginx 配置与实际部署不符

根目录 `nginx-dsh-updater.conf` 配置为 80 端口 + `/var/www/dsh-updater`，而实际预览部署在 8071 端口 + `/var/www/dsh-updater-preview`。另该配置开启 `autoindex on`，会对外暴露整个目录列表。

### P2-7 文档与合规细节

| 问题 | 说明 |
|---|---|
| README 结构过时 | 只列出 src-tauri/src 下 6 个文件，实际有 11 个（缺 catalog.rs、mcp.rs、snapshot.rs、version_probe.rs、bundle.rs） |
| 无 LICENSE | 代码已开源到 GitHub（Cargo.toml 声明 MIT），但仓库内无 LICENSE 文件 |
| 无 .github 目录 | 没有 CI 流程、无 SECURITY.md（安全披露渠道）、无 issue 模板 |
| 日期不一致 | CHANGELOG 标 1.13.8 为 2026-09-01，version.json 记录发布时间为 2026-09-02 |

---

## 五、审计结论

**做对的地方**：四个子项目版本号完全统一（1.13.8）；`proxy-server` 用环境变量读取 GitHub Token / Redis 密码，未硬编码；插件更新有 SHA256 校验与回滚机制；SQL 参数化、命令注入防护到位；`.gitignore` 对 node_modules / target / dist 的覆盖是正确的。

**核心风险**：整个安全链条建立在"私钥保密"这一前提上，而这个前提已经在 P0-1 中被打破。同时签名验证从未真正启用，使得风险从"可能被绕过"变成"根本没有防线"。

**建议处理顺序**：
1. 先轮换密钥 + 清理历史（P0-1），这是唯一的止血动作
2. 恢复或正式移除 `version.json`（P0-3）
3. 将 proxy-server 源码纳入版本控制（P1-1），防止资产丢失
4. 官网地址改配环境变量（P1-2），避免调试服务器下线导致线上事故
5. 密钥轮换完成后，再启用签名验证（P1-3、P1-4）
6. 最后做磁盘与文件归档清理（P2 各项）

> 第 5 步必须在第 1 步之后——顺序颠倒会导致用户端信任旧公钥，反而让轮换失效。
