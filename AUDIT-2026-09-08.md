# 项目自检审计报告

**审计日期**：2026-09-08
**审计范围**：`G:\DSH\DSH-PluginUpdater` 全仓库(src-tauri / src-vue / website / proxy-server / scripts / git 历史)
**审计方式**：只读取证(未修改任何业务文件),所有结论均有可复核的命令与路径
**代码版本**:工作区 1.14.0(未提交);HEAD/main = 1.13.15;CHANGELOG 最新条目 1.13.15

---

## 一、上轮审计(AUDIT-2026-09-03)处置结果核对

| 上轮问题 | 处置状态 | 核实证据 |
|---|---|---|
| P0-1 私钥泄露 | **部分处置,但留下新隐患** | 历史已重写:main 分支仅 22 个提交(全部始于 2026-09-03),旧泄露提交 `8932de1` 已不在 main 历史中;磁盘密钥已轮换为新密钥。**但轮换后的"新"私钥又被提交进了 `archived/fresh-snapshot-20260903` 分支**(见本轮 P0-1) |
| P0-2 旧报告虚假结论 | ✅ 已修复 | `AUDIT-REPORT.md` 顶部已加作废声明并指向新报告 |
| P0-3 version.json 丢失 | ❌ **状态恶化** | 见本轮 P1-1 |
| P1-1 proxy-server 不受版本控制 | ✅ 基本修复 | `.gitignore` 改为只忽略 `*.exe/*.mod/*.sum`,源码已入库。但 14MB 的 Linux 编译产物 `dsh-proxy`(无扩展名)绕过了忽略规则被提交(P2-1) |
| P1-2 官网硬编码调试 IP | ✅ 已修复 | 全站 grep `64.90.30.139` 无匹配(仅部署脚本保留,属既有问题) |
| P1-3 签名验证被注释 | ✅ 已启用 | `catalog.rs` 已实现 `verify_page_signature` + `verify_strict`,每页拉取并累积验证结果。**但验证失败仅 eprintln,不影响任何功能**(见本轮 P0-2) |
| P1-4 自更新无签名验证 | ❌ 未处置 | `check_self_update`/`self_update` 仍仅依赖服务器自证的 sha256(见 P0-3) |
| P1-5 安装包目录过时 | ✅ 部分修复 | 已更新至 1.13.8/1.13.9,但仍落后代码 1.14.0 约 5 个版本,且缺 1.14.0 安装包 |
| P2-1 16GB 构建缓存 | ⚠️ 略有收敛 | target 目录现约 6.1 GB(debug 4.05GB + release 2.03GB),历史清理提示有效但未清彻底 |
| P2-2 vision-router 截图 | ✅ 已清理 | git ls-files 无残留 |
| P2-5 部署脚本硬编码服务器 | ⚠️ 未变 | `scripts/push-via-relay.ps1:14` 仍硬编码 `root@64.90.30.139` + 私钥路径 |
| P2-7 无 LICENSE / .github | ❌ 未处置 | 仍无 LICENSE 文件、无 CI、无 SECURITY.md |

**总结**:9 项历史问题中 4 项完整修复、4 项部分修复或恶化、1 项未动。核心安全链路(签名验证)修了一半——验证代码启用了,但轮换流程本身又把新私钥提交进了分支。

---

## 二、本轮新发现问题

### P0 级(立即处理)

#### P0-1 轮换后的新私钥再次被提交进 git 分支

**事实链**:

1. 2026-09-03 的轮换提交 `1928f1e`(轮换第一版)与 `a1bb47d`(轮换最终版)把 `src-tauri/keys/ed25519-private.bin`、`scripts/ed25519-private.pem`、`scripts/key-info.json`(内含 PEM 明文私钥)**强制提交入库**——`a1bb47d` 的提交信息白纸黑字写着"添加 src-tauri/keys/ 中的二进制公钥/私钥"。
2. 随后 main 分支被整体重写(历史压缩为 22 个提交,不再包含密钥文件),但这两个轮换提交被**保留在了 `archived/fresh-snapshot-20260903` 分支**上。
3. 我用 SHA-256 逐字节比对确认:`archived` 分支 tip 上的 `ed25519-private.bin`、`ed25519-private.pem`、`key-info.json` 中的 PEM **与当前磁盘在用的私钥完全一致**(哈希 E7136D…)。
4. 该分支目前**未推送**到 hub(hub 仅有 main),但一条 `git push hub archived/fresh-snapshot-20260903` 就会再次把现行私钥送上服务器。

**业务影响**:与上轮 P0-1 完全同构——现行签名私钥在 git 对象库中处于"随时可能被推送/打包/镜像"的状态。`.gitignore` 里的 `src-tauri/keys/` 与 `scripts/ed25519-*.pem` 对**已在对象库中的内容无效**。

**处理建议**:
1. 立即把 `archived/fresh-snapshot-20260903` 分支上三个密钥文件的全部 blob 从历史中清除(`git filter-repo --path scripts/ed25519-private.pem --path src-tauri/keys/ed25519-private.bin --path scripts/key-info.json --invert-paths`),或至少确认该分支永不推送且不参与任何打包;
2. **再次轮换密钥**——该私钥已经躺在 5 天前的分支里,应按"已泄露"对待;
3. 给 `pre-push`/`pre-commit` 挂钩子,拒绝任何路径命中 `ed25519-private|keys/` 的 blob 进入远端。

---

#### P0-2 签名验证已启用,但**验证失败不拦截任何东西**

`main.rs:502-507`:

```rust
let (catalog_map, sig_valid) = build_catalog_map(proxy.http_client()).await;
if let Some(false) = sig_valid {
    eprintln!("[catalog] 签名验证失败，目录可信度存疑");
}
```

签名验证失败(= 服务器返回的目录签名与内置公钥不符,意味着数据被篡改或密钥不匹配)的唯一后果是**打一行 stderr 日志**,目录照常用于元数据填充、市场展示、更新检查(`check_updates` 里 `build_catalog_map` 的返回值直接被丢弃:`let (catalog_map, _) = ...`)。前端(types/index.ts、pluginStore)没有任何 sig_valid 字段——用户永远看不到"目录不可信"的提示。

结合 P0-3(更新链同样不验签),整个 Ed25519 体系目前是**装饰性**的:密钥在轮换、签名在计算、验证在执行,但验证结果对安全没有任何约束力。

**处理建议**:签名失败时应(至少)在 UI 层显著告警并拒绝用该目录元数据;长期应对签名失败的目录内容拒绝消费(降级到磁盘缓存)。

---

#### P0-3 自我更新链路完整性依旧形同虚设

`main.rs:1383-1440` + `2060-2179`:

1. `check_self_update()` 从 `https://dsh.huilinsh.cn/api/updater/latest` 拉版本信息,**无任何签名验证**;
2. `expected_sha256` 同样来自该接口——服务器被攻陷/域名被劫持时,恶意 exe 与配套 sha256 一起下发,校验必然通过;
3. 下载完成后 `cmd /c move /Y` 覆盖自身 exe,写失败(如装在 Program Files)时无任何检测与反馈;
4. `version.json`(版本清单源数据)已于提交 `34a4fd4` 从仓库**删除且未在任何地方重建**——见 P1-1。

上轮审计 P1-4 明确建议"对版本清单接口启用 Ed25519 签名",本轮代码未做任何改动。当前唯一实质防线是 HTTPS 传输层。

---

### P1 级(尽快处理)

#### P1-1 version.json 已被提交删除,自我更新通道无清单可依

- 提交 `34a4fd4`(2026-09-04,"白屏修复")把 `version.json` 从仓库删除(`D version.json`),此后 4 个提交无人恢复;
- 服务器端 `website/server/api/updater/latest.get.ts` 读取 `/var/www/dsh-updater/version.json`,**catch 后回退到 `version: '1.0.0'`**——文件缺失时所有客户端会被告知"最新版是 1.0.0";
- 最后一次在库的清单内容停留在 **1.13.9**(2026-09-03),而代码已是 1.14.0、CHANGELOG 到 1.13.15——即服务器清单(若与仓库同步)落后 6 个版本。
- 最新提交 `ad3a2b3` 声称"version.json 指向版本化文件",但该提交只改了 CHANGELOG,`version.json` 并不在仓库中,说明**发布流程与版本库已经脱节**。

**处理建议**:确认服务器 `/var/www/dsh-updater/version.json` 现状;把清单文件重新纳入版本控制(或建发布脚本从 CHANGELOG 生成),并把 fallback 版本从 '1.0.0' 改为"拒绝服务"而非诱导降级。

#### P1-2 两个整源码文件未被 git 追踪,工作区引用了它们

- `src-tauri/src/dsh_server.rs`(645 行,"本地 DSH Web 服务器管理"整个功能模块):**未跟踪**,但工作区 `main.rs:13` 已有 `mod dsh_server;` 并注册了 4 个命令。当前 HEAD 尚自洽(HEAD 的 main.rs 没引用它),**但下一次提交若只 add main.rs 忘了 add dsh_server.rs,仓库将无法编译**;
- `src-vue/src/components/ServerPanel.vue`:同样未跟踪,而工作区 `App.vue:144` 已 import;
- `website/pages/bundles.vue` + `website/composables/useMotion.ts`:未跟踪,但工作区 `SiteHeader.vue:16` 已把 `/bundles` 挂进导航、`index.vue:164` 已 import useMotion——**从当前 HEAD 克隆后,工作区这套改动一旦提交,漏掉任何一个文件官网就缺页/白屏**。

这 37 个未跟踪文件 + 35 个未提交修改(含图标全量更换、版本号 1.13.15→1.14.0)构成了一个**巨大的未提交变更集**,已持续 4 天(最后提交 2026-09-04)。版本一致性风险随时可能变成事故。

#### P1-3 版本号三处脱节

| 位置 | 版本 |
|---|---|
| 工作区 package.json × 4 + tauri.conf.json + Cargo.toml | **1.14.0**(未提交) |
| HEAD/main 的上述文件 | 1.13.15 |
| CHANGELOG 最新条目 | 1.13.15(2026-09-04,无 1.14.0 条目) |
| 安装包目录 | 1.13.8 / 1.13.9 |
| version.json(已删) | 最后记录 1.13.9 |

桌面端 `check_self_update` 用 `CARGO_PKG_VERSION`(=1.14.0)与服务器清单比较,而服务器清单反映的是实际分发版本——**代码版本 ≠ 发布版本 ≠ 分发文件版本**,自我更新的版本比较基础已不可靠。

#### P1-4 官网插件目录签名算法与桌面端验证算法不匹配(签名永远验不过)

- **官网**(`plugins/index.get.ts:127-141`、`manifest/sign.post.ts`):用 `createSign('SHA256')` 对 `sha256(canonical JSON)` 签名——这是 **RSA/ECDSA 风格的 Sign 对象 API**;
- **桌面端**(`catalog.rs:596-615`):`ed25519_dalek` 的 `verify_strict(body_bytes, sig)`——对**原始响应体字节**做 Ed25519 验证;
- Node 的 `crypto.createSign` **不支持 Ed25519**(`sign.sign()` 会直接抛错),而官网代码里确实 `catch {}` 静默吞掉(注释:"私钥不可用时静默跳过")。

**推论**:官网 `/api/plugins` 的 `X-DSH-SIGNATURE` 头**要么永远不出现(createSign 抛错被吞),要么(即便换成可用的 API)签的内容是 canonical JSON 而客户端验的是原始 body,两边必然不匹配**。桌面端 `sig_valid` 将恒为 `None`,P0-2 的"验证失败告警"永远不会触发——因为验证根本无从发生。

**处理建议**:官网改用 `crypto.sign(null, data, ed25519PrivateKey)`(Ed25519 原生 API)对**原始响应体字节**签名,与桌面端 `verify_catalog_signature(sig, body_str, SIGNING_PUB_KEY)` 的口径对齐;两端补一组端到端测试。

#### P1-5 桌面端硬编码开发者本机绝对路径

`src-tauri/src/dsh_server.rs`:

- `DSH_HARNESS_DIR = "G:/DeepSeek_Harness"`、`DSH_CLI_DIR = "G:/DeepSeek_Harness/apps/cli"`(第 35-37 行)
- `DSH_WEB_PROFILE_DIR = "C:/Users/niufe/.dsh/profiles/web"`(第 43 行)
- `find_node()` 硬编码 `C:/Users/niufe/.workbuddy/binaries/node/versions/22.22.2...`(第 548-549 行)

这个模块对外提供 `server_start` 功能——**在任何其他用户的机器上都会因找不到 G:/DeepSeek_Harness 而直接报错**,node 探测也优先踩一个不存在的路径。README 声称"支持 Windows 和 Linux",而 dsh_server.rs 的常量全是 Windows 开发机专属路径(非 Windows 分支从未被认真实现)。应改为:DSH 安装目录让用户配置/自动探测(`~/.dsh`),node 用 `which node` + 版本探测,profile 目录用 `dirs::home_dir()`。

#### P1-6 目录拉取存在放大效应与循环上限硬伤

`catalog.rs:165-189`:分页循环 `for page in 1..=30`,每页 `page_size=200`,理论上限 6000 条;但**循环内没有 total 保护之外的终止错误处理**——若某页请求失败(网络抖动),`verify_page_signature` 直接 `?` 返回 Err,整个目录拉取失败(有磁盘缓存兜底,但市场会变空)。另外 `enrich_npm_downloads` 之前修过"2100 条全量风暴"问题,当前仅对 `downloads` 缺失的条目补拉——但**首次官网目录缺失 downloads 字段时仍会并发 8 路打 api.npmjs.org**,若官网 full 模式正常此路径不触发,风险仅在降级链路。

---

### P2 级(计划处理)

#### P2-1 14MB Linux 二进制 `proxy-server/dsh-proxy` 提交进了 git

`.gitignore` 只忽略 `proxy-server/*.exe`,而 Go 交叉编译产物 `dsh-proxy`(Linux,14,672,272 字节)无扩展名,于 `967a088` 入库且仍在 HEAD。`.git` 目录因此膨胀到 11.2MB(对一个 276KB 源码的项目)。应改为忽略 `proxy-server/dsh-proxy*` 并从历史清除,或加 `!` 白名单只保留源码。

#### P2-2 根目录散落 16MB 产物与双份图标包

- `DSH-Icons-v3.0-DSH风格.zip`(7.4MB)+ `dsh-plugin-butler-1.14.0-x64-setup.exe`(4.5MB)+ `DSH-PluginUpdater-Icons-v2.0.zip`(3.9MB)——均为可再生产物;
- 解压后的 `DSH-Icons-v3.0-DSH风格/`、`DSH-PluginUpdater-Icons-v2.0/`(11.1MB)与 `icon-build/`(5.4MB)未跟踪也未忽略,`git status` 里漂着;
- `src-vue/dist-old/`(0.5MB,历史 dist 副本)、5 个 `vite.config.ts.timestamp-*.mjs` Vite 临时文件(其中一个 timestamp-1788402517125 居然已被 git 追踪);
- `安装包/` 40.2MB,含 1.13.8 msi/exe 与 1.13.9 两套,仍无 1.14.0。

#### P2-3 website 鉴权细节

- `auth.ts:10-11`:`JWT_SECRET` 未设环境变量时用随机值——**重启即全员登出**,注释已自知但代码放行。建议启动时若未显式配置则拒绝启动或只允许开发模式;
- `login.post.ts:13`:`x-forwarded-for` 头直接信任取 IP,nginx 未强制覆盖时可被伪造绕过 IP 限频(邮箱维度仍在);
- cookie 未设 `secure` 标志(https 部署下应加)。

#### P2-4 桌面端遗留风险点

- `main.rs` 87 处 `.unwrap()` 中约 60 处在生产命令路径上(多为 Mutex lock,poisoned 即 panic 崩溃);
- `plugin/download.get.ts:75`:`X-DSH-SHA256` 头名下发的其实是 **SHA1 shasum**(npm dist.shasum),客户端 `update_plugin` 靠长度 40 判断回退 SHA1 已兼容,但头名是误导;
- `github_proxy.rs` 默认 `proxy_base_url` 为空串,README 说"填写代理后所有 GitHub 请求经代理中转"与实现一致,但 `check_auto_update_background` 里 `format!("{}/api/updater/latest", proxy_url)` 在**代理为空时会拼出 `/api/updater/latest` 相对地址导致请求必然失败**(自动检查更新在默认配置下悄悄失效,靠 `check_self_update` 的硬编码域名兜底);
- `tauri.conf.json` `csp: null`——Tauri 官方明确不建议生产环境留空 CSP。

#### P2-5 README 与实际结构不符

README 项目结构只列 6 个 Rust 模块,实际已 11 个(soon 12,含未提交的 dsh_server.rs);"官网 2189+ 款插件"等数字应与 `getPlugins` 数据源对齐说明;MIT 许可证声明(README + Cargo.toml)但**仓库无 LICENSE 文件**,GitHub 上法律效力存疑;无 `.github/`(CI/SECURITY.md/issue 模板)依旧缺失。

---

## 三、做得好的地方

1. **历史重写执行到位**:main 分支 22 个提交全部始于 2026-09-03,旧泄露提交、vision-router 截图、dist 残留全部清除;.gitignore 对 key 的规则先于提交生效(这次顺序对了);
2. **更新事务链完整**:备份→下载→校验(SHA1/SHA256 自适应)→兼容预检(fail-open 合理)→解压→**路径穿越防护**(`sanitize_rel_path` 拒绝 `..`/盘符/根,tgz 与 zip 双路径覆盖)→版本回写→锁文件同步→失败回滚,且有真实单元测试(tests 里构造恶意 tar/zip 验证);
3. **npm 安装链路工程化**:绕过 cmd 中转直连 node+npm-cli.js、CREATE_NO_WINDOW、瞬时失败重试、镜像缺版本回退官方源、workspace: 毒包人话诊断——都是踩坑后修出来的;
4. **扫描逻辑经过真机校准**:`@deepseek-ai` scope 排除、`dsh-profile-*` 宿主排除、keyword 限定 `-plugin` 后缀防误收 238 个依赖包,均有数据佐证注释;
5. **官网侧安全基础**:better-sqlite3 全程参数化(抽查 8 个 API 无一拼接)、登录双维度滑动窗口限频、下载域名白名单、admin 角色逐端点校验、feedback 带 CHECK 约束;
6. **备份与回滚**在卸载/更新两条路径上都默认开启。

---

## 四、建议处理顺序

1. **止血(P0-1)**:清除 archived 分支密钥 blob + 再轮换一次密钥 + pre-push 钩子。这是唯一有时间窗口的动作——分支尚未推送;
2. **修签名链(P0-2 + P1-4)**:官网改 Ed25519 原生 API 对原始 body 签名 → 客户端签名失败时 UI 告警并降级到磁盘缓存;
3. **救自我更新(P0-3 + P1-1)**:version.json 重新入库/入服务器,updater 接口接入 Ed25519 签名(私钥轮换完成后),fallback 从 '1.0.0' 改为不可用标记;
4. **提交工作区(P1-2 + P1-3)**:把 72 个文件变更整理成有意义的提交(确保 dsh_server.rs、ServerPanel.vue、bundles.vue、useMotion.ts 全部入库),补 CHANGELOG 1.14.0 条目,打 1.14.0 安装包;
5. **去硬编码(P1-5)**:dsh_server.rs 路径改探测/配置;
6. **卫生清理(P2 全部)**:忽略并清除 14MB dsh-proxy、归档根目录 zip/exe/安装包、dist-old、vite timestamp、补 LICENSE 与 .github。

> 顺序不能颠倒:第 4 步若先于第 1 步执行,整理提交时的 `git add -A` 极易把磁盘上的密钥文件再次带进库(.gitignore 会挡住,但 archived 分支里的对象仍在,先清历史再大批量操作更安全)。
