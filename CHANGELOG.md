# 更新日志（Changelog）

*格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)，发布流程见 [docs/VERSIONING.md](docs/VERSIONING.md)。*

## [Unreleased]

### Changed（官网内容全面更新）

- **移除在线版入口**：在线版已于 v1.13.15 下线，官网头部/首页/下载页/插件安装弹窗/页脚全部清除 `previewUrl` 死链（配置项保留空占位）；插件安装弹窗第二选项改为「按行业整套安装」（指向组合包页）
- **首页改版**：Hero 徽标改为「v1.14.0 · 安全加固版」；统计条新增「99+ 行业组合包」「签名保护」；双端形态区改为「桌面客户端（含组合包/MCP/快照/服务器面板）+ V3 安全体系」卡片；核心能力 6 项扩为 8 项（新增组合包安装、签名验证、MCP 服务管理、快照与离线、服务器面板）
- **导航与页脚**：头部导航新增「组合包」入口；页脚品牌描述、产品/资源列更新（新增组合包与离线部署指引链接），版权行改为 MIT License + 签名验证提示
- **文档页大幅扩写**：新增「行业组合包」「快照与离线部署」「DSH Web 服务器面板」「安全设计（验证范围表 + 密钥管理 + 过渡语义）」「自动更新」五章；快速开始/安装指南同步 1.14.0 语义（SHA256 校验提示、npm/GitHub 双源）；FAQ 新增签名失败告警解释与「在线版去哪了」；代理部署章节标注「自建可选」（默认直连）
- **下载页清理**：移除在线版优先横幅；系统要求新增「安全」行（Ed25519 验签 + SHA256 校验）；首次使用引导改为组合包/MCP 路径
- **/updater 孤儿页下线**：删除 1.0 时代营销页（含假统计数字），`routeRules` 将 `/updater` 301 到首页；同步删除 `pages.bak/` 旧版备份目录
- **SEO 元信息更新**：description/keywords 覆盖组合包、MCP、离线部署、签名验证关键词；新增 og:title/og:description/og:type；组合包页补独立 title + description
- **pnpm-workspace.yaml**：`allowBuilds` 明确允许 better-sqlite3 / esbuild 构建脚本（修复 `pnpm exec nuxt build` 因 deps-status-check 触发 install 被 ERR_PNPM_IGNORED_BUILDS 拦截的问题）

## [1.14.0] - 2026-09-08

### Security (V3 签名链全量修复)

- **Ed25519 签名链贯通**：官网 `/api/plugins` 与 `/api/updater/latest` 对原始 body 字节签名（`node:crypto.sign(null, body, pem)`），桌面端编译时嵌入公钥验证（`verify_page_signature` / `verify_response_signature`）；签名缺失 → fail-open，签名失败 → fail-closed
- **目录签名验证失败强制拦截**：`fetch_catalog_from_website` 返回 `sig_valid=false` 时拒绝消费目录数据，降级磁盘缓存；缓存不可用时返回错误而非空列表
- **密钥第三次轮换**：新 Ed25519 密钥对已生成并部署到全部位置；`git filter-repo` 从全部引用（main + archived + 32 个 tag）清除旧私钥 blob、key-info.json、dsh-proxy 二进制与 vite 临时文件；`.git` 大小从 11.2MB 降至 3.6MB
- **pre-commit / pre-push 钩子**：`scripts/git-hooks/` 版本化防泄漏钩子，阻止私钥文件被意外提交或推送
- **官网 JWT fail-fast**：`NODE_ENV=production` 下缺失 `DSH_JWT_SECRET` 时服务启动即抛出异常，避免随机密钥导致的 session 劫持
- **Cookie Secure flag**：登录/注册/GitHub 回调/登出全部 cookie 在生产环境启用 `Secure`
- **clientIp 优先级修正**：优先读 Nginx `X-Real-IP`（不可伪造），再 fallback 到 `X-Forwarded-For` 首段
- **X-DSH-SHA256 改名为 X-DSH-SH1**：消除 MD5 弱化暗示，与 SHA256 语义对齐

### Features

- **dsh_server 路径去硬编码**：`DSH_HARNESS_DIR` / `DSH_CLI_DIR` / `DSH_WEB_PROFILE_DIR` 改为自动探测（环境变量 `DSH_WEB_DIR` > `~/.dsh/web-dir` 缓存 > 候选路径），`find_node` 遍历 `.workbuddy/binaries/node/versions` 按版本号取最新 22.x
- **version.json 恢复**：重新纳入版本清单（含 sha256、平台 URL、changelog）；fallback 改为 `0.0.0` 杜绝误降级
- **自更新代理 URL 兜底**：`proxy_url` 为空时降级到官方直链，不再直接报错
- **安装包归档整理**：旧版本移入 `安装包/旧版本/`，新增 1.14.0 安装程序

### Fixes

- **官网签名 API 兼容性**：`manifest/sign.post.ts` 与 `manifest/verify.get.ts` 改用 Ed25519 原生 API（移除不兼容的 `createSign('SHA256')` wrapper）
- **根目录 package.json**：补充 `name` / `private` 字段，避免 npm 命名冲突

### Docs & Hygiene

- **LICENSE**：新增 MIT 许可证
- **.github/**：新增 `SECURITY.md` + CI workflow（cargo check + npm build）
- **gitignore 重构**：扩展忽略规则（icon-build/、DSH-Icons-v*/、dist-old/、vite timestamp 文件、proxy-server/dsh-proxy），移除 `src-tauri/keys/` 整目录忽略（改为仅忽略私钥文件）
- **README 更新**：项目结构补充 dsh_server/catalog/bundle/snapshot/mcp 模块说明，新增安全设计章节与密钥配置说明

## [1.13.15] - 2026-09-04

### Added

- **组合包库扩充（94 个行业预制组合包）**：将 docs/DSH_INDUSTRY_PLUGIN_MCP_SKILL_COMPLETE.md 的全部预制组合包入库官网 bundle 种子——通用基础 4 个 + 13 大行业组（医疗/法律/金融/教育/电商/制造/房地产/媒体/政务/IT/农业/物流/旅游等，含殡葬与生命服务），共 281 条插件引用、284 个 MCP 服务模板、282 个 Skill。组合包标签页与官网 /api/bundles 即时可见。
- **组合包索引分页拉全量**：桌面端 list_bundles 此前仅取默认页 50 条，现按 total 循环分页（page_size=200）拉全量。
- **MCP 模板预览增强**：command 为空的模板显示「需手动配置（见说明）」并展示功能说明文字。
- **下载文件名带版本号**：分发文件改为 dsh-plugin-updater-<版本>.exe（nginx 正则映射），version.json 的 platforms.windows.url 指向带版本文件名；不带版本号的 /dsh-plugin-updater.exe 保留为最新版别名。

## [1.13.14] - 2026-09-04

### Fixed

- **已安装列表 236 个插件（回退 1.13.13 下钻误伤）**：容器目录 node_modules 下钻把 DSH Agent 本体工作区的 ~213 个 @deepseek-ai/dsh-* 运行时组件与 2 个 profile 根（dsh-profile-*）全部误判为插件。现扫描跳过 @deepseek-ai scope（本体组件非插件）并拒绝 dsh-profile-* 根目录，真实插件 ~22 个。真机 dump 验证：desktop 22 → 修复后仅保留真实插件。
- **更新确认弹窗（第 3 次反馈）**：DSH 运行中点更新此前弹出 WeUI 底部动作面板（贴底边缘、观感如同样式丢失），现改为居中确认对话框：确认结束 DSH → 失败时可选强制继续，全程居中样式。

## [1.13.13] - 2026-09-04

### Changed

- **已安装列表全量显示**：不再隐藏内置/系统插件，bundled 显示「内置」角标、agent-core 显示「系统」角标，一眼区分来源。
- **扫描覆盖 pnpm workspace 根 node_modules**：容器目录下钻 node_modules 顶层包（含 @scope/*），检出 profiles/node_modules 中的独立插件（此前 scan_nested_plugin 只返回首个命中，@wingsky-1/dsh-mcp-manager 0.2.0 等被漏掉——dsh-market 可见而桌面工具查不出的主因）。
- **更新检查纳入 dshmarket**：仅 @deepseek-ai/* 本体组件跳过 npm 检查，dshmarket 等独立发布的 agent-core 类型插件同样参与。

### Removed

- **在线版下线**：8071 预览站点停止服务，官网移除「在线预览」入口，前端浏览器 Mock 层整体删除（pluginApi 仅保留 Tauri invoke 实现），杜绝假数据混淆。

## [1.13.12] - 2026-09-04

### Fixed

- **可更新插件可见性**：已安装列表改为「用户插件 + 有更新的内置插件」——bundled 插件默认隐藏，但检出可用更新时显示并带「内置」标记，检查出 5 个可更新却找不到插件的问题解决；市场卡片/行同步增加「可更新」角标，置顶排序一目了然。
- **在线版市场只显示 48 个**：浏览器分支拉目录未传 page_size（服务端默认页 48 条），现分页全量拉取（page_size=200 循环至 2189 条），与桌面端同源同量。
- **在线/桌面数据不一致**：在线预览的「已安装」此前展示 mock 假插件（含假更新），现改为返回空列表并在空状态明确提示「浏览器无法读取本机插件，请使用桌面端」，市场与桌面端同源。
- **头部图标无悬浮提示**：tooltip 原实现向上弹出，头部按钮贴窗口顶边导致提示弹到窗外不可见；现头部按钮提示向下弹出。
- **内置标记补充**：内置插件在已安装列表显示「内置」角标（悬停说明：本体预装插件，由 DSH 本体统一管理）。

## [1.13.11] - 2026-09-04

### Fixed

- **已安装列表过滤内置插件**：v1.13.8 声称的「已安装列表仅展示用户插件」实际未生效——`filteredPlugins` 未排除 bundled/agent-core，21 个本体预装插件全部显示。现已统一过滤（列表、tab 计数、分类统计三处），bundled 插件的更新提示保留在市场列表置顶高亮中。
- **在线版 HTTPS**：8071 端口预览站此前仅 HTTP，浏览器自动升级 HTTPS 后无法访问；nginx 已为 8071 配置 SSL 证书（与主站同证书），HTTP 请求 301 跳转 HTTPS，「在线预览」链接同步改为 https。

## [1.13.10] - 2026-09-04

### Fixed

- **软件白屏修复（关键）**：v1.13.9 的 pluginStore 导出了 `getAutoUpdateState` / `launchAutoUpdate` 但两个函数从未定义，store 初始化时抛 ReferenceError 导致 Vue 挂载失败白屏。现已补齐实现（invoke 后端同名命令），并通过 vue-tsc 全量类型检查。
- **在线版「在线预览」链接修复**：previewUrl 原默认值指向 /updater/ 落地页自身（死循环），现修正为真实在线版地址 http://dsh.huilinsh.cn:8071/。
- **后台自动下载修复**：download_auto_update_background 曾把 reqwest chunk 返回的数据丢弃、写入未填充的旧 buffer（下载文件必损坏），现直接写入真实分块。
- **自动更新状态补全**：AutoUpdateState 增加 temp_path 字段，下载完成后前端悬浮窗可直接「立即安装」；前端监听 auto_update_check / progress / done 三个事件并在启动时主动拉取一次状态（避免事件早于监听建立而丢失）。

## [1.13.8] - 2026-09-01

### Changed

- **bundled 插件重新计入可更新列表**：bundled 插件（随本体 profile 声明）现在正常参与更新检查并显示在可更新列表中。注意：更新 bundled 插件后，DSH Agent 重启时可能被本体重装覆盖（本体 bundles 清单说了算）。

## [1.13.7] - 2026-09-01

### Fixed

- **bundles 内非 dsh-* 前缀插件可被识别**：`@liustack/modlens` 等插件名字不含 `dsh-` 前缀、keywords 也无 `dsh-plugin`，此前被扫描逻辑过滤掉导致工具看不到；现把 `is_declared_plugin`（bundles/dependencies 声明判定）提到识别逻辑最前面，确保随本体分发的插件即使名字特殊也能被正确识别并参与更新检查。

## [1.13.6] - 2026-09-01

### Fixed

- **已安装列表多出插件修复**：`auto_scan_plugins` 把 `.dsh/profiles` 容器目录当作候选源扫描，导致 `profiles/node_modules` 下的 `dsh-community-market`、`dsh-plugin-desktop` 等被重复识别；同时 `is_dsh_profile` 对容器目录误判为独立 profile。现增加容器目录兜底判断：若目录是 `profiles` 的子目录且包含标准子 profile（desktop/web/cli/mobile），则不视为独立 profile。
- **bundled 语义回归**：v1.13.5 曾将 bundled 插件重新计入可更新列表，现回退至 v1.13.0 语义——bundled 插件（随本体 profile 声明）跳过 npm 更新检查并排除在可更新列表外。

## [1.13.5] - 2026-09-01

### Reverted

- **回退 bundled 插件显示**：v1.13.0 将 profile 根 dependencies/bundles 声明的预装插件标记为 bundled 并跳过更新检查；v1.13.5 曾改为显示 bundled 更新，但用户反馈「已安装列表多出很多插件、用户使用会弄不明白」——回归 bundled 排除语义（bundled 更新由 DSH Agent 本体统一管理）。bundled 字段仍保留供后续 UI 灰度标签用。

## [1.13.4] - 2026-09-01


### Fixed

- **MCP 连通性预检不再误导**：streamable-http 服务返回 404/405（URL 不是 MCP 端点）此前被误判为「服务可达」——现明确提示「该路径不是 MCP 端点，检查 URL 路径（通常以 /mcp 结尾）或服务未挂载该路由」；401/403 提示补全鉴权。

## [1.13.3] - 2026-09-01

### Fixed

- **组合包版本不符修复**：版本探测与安装此前可能经过不同的 npm 镜像源（同步进度不同），安装 latest 时拿到旧版本导致「期望 3.25.4、实际 3.25.0」校验失败——现按组合包期望版本精确安装（pkg@version）；镜像源缺少该版本时自动回退官方源，确保装到的版本与期望一致。离线包导入同步修正为按快照记录版本精确还原。

## [1.13.2] - 2026-09-01

### Fixed

- **组合包安装退出码 0xC0000142 根治**：此前 npm 安装经 cmd.exe 两层中转派生进程，GUI 程序在系统资源紧张时子进程 DLL 初始化直接失败（退出码 -1073741502、零输出、重试无效）——改为直接以 node.exe 运行 npm-cli.js（探测 npm.cmd 同目录布局，失败自动回退 cmd 路径），并附加 CREATE_NO_WINDOW 标志，进程链从三层减为一层，绕开桌面堆分配失败。

## [1.13.1] - 2026-09-01

### Fixed

- **组合包安装韧性**：npm 安装瞬时失败（npm 缓存锁、杀毒软件扫描抖动）自动重试一次；安装目标目录不存在时不再把无效路径作为工作目录；npm 无任何输出即失败时，报错补齐诊断上下文（退出码与排查建议），不再出现「npm install 失败: 」的空报错。

## [1.13.0] - 2026-09-01

### Added

- **本体预装插件不参与更新检查**：被 DSH 本体 profile 依赖/bundles 声明的预装插件（dsh-mcp-manager、dsh-cloud-sync、modlens 等）标记为「随本体分发」——npm 上游更新由 DSH 本体统一管理，工具不再提示更新、不计入可更新数量，避免更新后被本体重装覆盖；检查更新也跳过这些插件（更快）。

## [1.12.2] - 2026-09-01

### Fixed

- **卸载插件失败修复**：卸载时的目录删除在 Windows 上对只读文件、深层依赖路径（超 260 字符）及被运行时短暂占用的文件会直接报错——改为稳健删除（清只读属性 → 常规删除 → 长路径前缀重试 → 等待占用释放重试），仍失败时给出明确提示（可能正被 DSH Agent 占用，关闭后重试）。卸载前备份保持不变。

## [1.12.1] - 2026-09-01

### Fixed

- **插件列表加载提速**：官网目录已自带 npm 月下载量，客户端仍在目录刷新时无条件对全部 2100+ 条目并发请求 npm 下载接口（20 秒预算的请求风暴且永远拉不完）——改为仅对缺失下载量的条目兜底补拉，官网目录命中时零外网请求，市场列表秒开。

## [1.12.0] - 2026-09-01

### Added

- **检查更新性能重构**：新增官网批量检查端点（/api/updater/batch-check），客户端一次请求完成全部插件的 npm 版本对比——此前逐插件串行查询 20 个插件需 20-40 秒，现降至约 1 秒；端点不可用时自动回退本地并发查询（8 路），镜像链兜底保持不变。

## [1.11.0] - 2026-09-01

### Added

- **离线部署指引页**：官网新增「离线部署」导航与指引页（导出离线包 → 物理转移 → 离线机导入三步说明），dshupdater://offline 动作直达客户端「快照与离线打包」面板；下载中心加入口链接。

### Fixed

- 插件市场搜索栏占位文字过大在窄容器下强制换行，撑高工具栏——字号对齐输入框并禁止换行（超长省略号截断）。

## [1.10.0] - 2026-09-01

### Added

- **dshupdater:// 协议唤起**：工具启动时自动注册唤起协议（HKCU，免管理员，便携 exe 无需安装器）。官网下载中心新增「打开工具」「检查插件更新」按钮，浏览器一键唤起桌面客户端；工具已在运行时自动聚焦现有窗口（单实例），「检查插件更新」动作直达更新检查。

## [1.9.0] - 2026-09-01

### Added

- **工具栏吸顶浮窗**（高级质感）：下滚插件列表时，Tab/搜索/排序工具栏与分类筛选行自动吸附顶部并渐变为玻璃浮窗（半透明模糊背景 + 阴影 + 圆角胶囊），离开顶部平滑还原；浮入瞬间带轻微下沉回弹动效，尊重系统「减少动态效果」设置。宽屏与移动端布局均生效。

## [1.8.0] - 2026-08-31

### Fixed

- **已安装统计虚高**（真机 238 个）：扫描器裸关键词匹配把官方依赖包整树误收为插件——关键词限定到 dsh-plugin/deepseek-harness-plugin 后缀、目录名入口限定 dsh- 前缀；已安装数现只统计真实插件，头部与「已装」标签同口径排除系统内置。
- **系统内置不再计入**：@deepseek-ai/* 与内置市场 dshmarket 标记为本体类型，统计与更新列表自动排除。
- **可更新数与市场口径不一致**（市场 9 vs 本地 1）：检查更新的 npm 包名解析在目录未命中时直接以插件包名兜底，全部插件走 npm registry 实时对比，不再落入 GitHub 对比导致漏报。
- 弹窗底部按钮错位修复（1.7.0）随本版全量下发。

### Changed

- **插件市场统计改为目录全量**：此前固定拉取前 200 条，现循环分页拉取完整官方目录（服务端单页上限 200）。

## [1.7.0] - 2026-08-31

### Fixed

- **修复弹窗按钮竖排错位**（全面自检全部弹窗）：WeUI 原生按钮 `min-width:184px` 使弹窗底部双按钮（184×2+间距=378px）超出 360px 弹窗容器触发换行堆叠（实测两按钮垂直相距 48px）；现弹窗底部按钮统一自适应宽度（最小 96px），所有弹窗实测单行对齐、无内容溢出。

### Added

- **MCP 服务管理、快照与离线打包提升为一级入口**：顶部工具栏新增两个直达按钮（移动端「更多」菜单同步），无需再进入设置查找；设置内入口保留。

## [1.6.1] - 2026-08-31

### Fixed

- **自更新包完整性核对（验收冲刺修复）**：程序内「检查更新→立即更新」下载新版本后此前未校验 SHA256 即替换，现强校验服务器提供的校验和，不符即拒绝安装并清理临时文件；下载失败同样清理。

### Added

- **半装检测与引导恢复**（V2 §9 验收 #2）：组合包安装事务若被中断（进程被杀/断电），重启时自动检测台账中的 running 残留记录，优先从备份恢复受影响插件（无备份则清理半装目录），并原位转换为 recovered 审计记录；
- 验收补测：verify 后取消仲裁（#3）、回滚失败保留备份并给出人工恢复提示（#4）。

## [1.6.0] - 2026-08-31

### Added

- **快照导出/导入（P1 尾项）**：把当前已装插件清单（名称/版本/npm 包名）导出为 JSON 快照；导入时先预览与当前目录的差异（缺失/版本不同），可一键在线安装缺失插件；
- **离线打包/还原**：把全部插件目录打包为 zip（含依赖），在无网络环境原样还原（同名文件覆盖）；
- 设置对话框新增「快照与离线包」入口（快照与离线打包对话框）。

## [1.5.0] - 2026-08-31

### Added

- **预设模式适配（P1）**：`mode=preset` 组合包安装时插件照常安装，但 MCP 服务与技能**不再写入全局配置**，改为生成「会话预设建议」文件（`%APPDATA%\dsh-plugin-updater\preset-suggestions\<包id>.json`），由会话按需选择性启用——避免把全部 MCP/Skill 注入全局上下文造成 Token 过载；
- 组合包详情对话框为预设模式包显示「预设模式」徽章与说明；安装完成消息包含建议文件路径。

## [1.4.2] - 2026-08-31

### Added

- MCP 服务管理面板内置使用说明（可折叠，四步指引）；官网文档中心新增「MCP 服务管理」章节。

## [1.4.1] - 2026-08-31

### Fixed

- 修复左上角版本号徽章不显示：Tauri v2 能力授权缺失导致版本号读取被静默拒绝，现已补齐默认能力授权，徽章正常显示 v1.4.1。

### Added

- 设置页底部显示当前版本号，与左上角徽章同源（应用自身版本）。

## [1.4.0] - 2026-08-31

### Added

- **MCP 服务管理面板**（设置 → MCP 服务管理）：集中查看与管控本地 MCP 配置（`~/.dsh/dsh-mcp.json`），用户手工添加的条目始终保留；
- **环境变量密值加密存储**：面板填写的 Token/密钥保存在系统凭据库（Windows 凭据管理器，加密存储），面板一律掩码显示；点击「写入配置」才写入运行配置供 DSH 使用；
- **连通性预检**：stdio 服务一键拉起探活（能启动即可用），streamable-http 服务发起探测请求确认可达（401/403 视为可达但需配鉴权），返回耗时；
- **单服务启用/禁用**：禁用条目安全移出运行配置并暂存，随时一键恢复；其余条目（含手工条目）零改动。

## [1.3.0] - 2026-08-31

### Added

- **组合包预检增强**：查看组合包时自动比对官网冲突知识库——包内插件与当前已安装插件的已知冲突会在预览中逐项标注，存在阻断级冲突时给出醒目警示，安装前即可发现问题；
- **更新包完整性核对**：检查更新时同步获取 npm registry 发布时登记的校验和，更新包下载完成后自动核对，不一致即拒绝安装并回滚，防止镜像劫持或传输损坏。

### Changed

- 官网新增组合包整包预检接口（`/api/bundles/compat/check`），一次请求返回包内全部插件的兼容与冲突结论。

## [1.2.0] - 2026-08-31

### Added

- **下载中心动态化**：版本号、安装包大小、SHA256 与下载链接改为从官方更新通道实时读取，发布新版本后下载页自动跟随，不再需要手改页面；
- 官网新增技能索引接口（`/api/skills`）与 MCP 服务模板索引接口（`/api/mcp`）：展示模板所需的全部信息，环境变量仅列键名与用途，值始终由本地填写。

### Fixed

- 移除下载页 Linux 版本的无效占位下载链接：未提供安装包时明确标注「暂未提供」，不再给出 404 链接。

### Changed

- 系统要求文案改为中性表述（访问 GitHub 与 npm 官方源，内置直连加速）。

## [1.1.0] - 2026-08-31

### Added

- **组合包一键安装**：官网新增组合包市场（5 个官方组合包），桌面端市场页新增「组合包」标签页；支持按组合包预检（已装/待装/已是最新）、一键安装、分阶段进度（预检/备份/下载/安装/校验/提交）与安装中取消；
- **事务化安装保障**：安装前自动备份将被覆盖的插件，任一步失败自动回滚到安装前状态；回滚失败时保留备份，绝不破坏现有环境；安装完成后自动合并组合包内的 MCP 服务模板到本地 MCP 配置（既有条目零丢失，环境变量只登记键名，值由本地填写）；
- 组合包内的技能（Skill）清单随安装一并展示；
- **DSH 运行时版本探测**：环境检测新增「DSH 运行时版本」一项，组合包可声明最低版本要求；
- 桌面端与官网左上角品牌区显示应用版本号徽章。

### Security

- 修复插件包（zip/tgz）解压的目录穿越风险：恶意构造的压缩包（`../` 相对路径或绝对路径条目）不再可能把文件写到插件目录之外；异常条目跳过并记录日志，不影响其余文件解压。

### Changed

- 官网组合包索引接口支持条件请求（ETag / 304），客户端增量拉取更省流量。

## [1.0.0] - 2026-08-28

### Added

- **桌面客户端**：独立运行的插件升级管理工具（Tauri 2 + Vue 3），不依赖 DSH 本体；
- 插件扫描：目录选择框定位插件目录，自动识别已安装插件并支持多 profile 候选与去重；启动时自动扫描；
- **插件市场**：全部市场 / 已安装 / 可更新三标签页，官方目录源 + 磁盘缓存，双语介绍；
- 一键安装 / 单插件更新：更新前自动备份旧版本、安装后版本校验、失败自动回滚、下载进度实时显示；
- 安全卸载：卸载前自动备份；一键打开插件所在文件夹；
- **官网正式上线**：插件市场、下载中心、使用文档、反馈系统（GitHub 登录 + 管理后台）；
- 服务器同步：配置服务器后可推送安装包、发布目录与更新元数据；
- 亮 / 暗主题、中英双语、设置面板、修复工具；
- 自更新检查：启动时检查新版本并引导下载。

### Changed

- GitHub 访问统一走官网数据源与 npm 官方源，公开配置仅使用中性表述。
