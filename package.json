{
  "name": "dsh-plugin-updater",
  "version": "1.15.0",
  "private": true,
  "scripts": {
    "build": "cd src-vue && npm run build"
  },
  "dependencies": {
    "@deepseek-ai/dsh-settings": "^0.1.0-rc.8",
    "@deepseek-ai/dsh-tools": "^0.1.0-rc.8"
  }
}
